<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/base.xml" />
    <!-- Spring is verbose on the INFO level, so raise it to WARN -->
    <logger name="org.springframework" level="WARN"/>
</configuration>
