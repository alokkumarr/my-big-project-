<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>xdf-nextgen</groupId>
    <artifactId>bigdataframework</artifactId>
    <version>1.0.0</version>
  </parent>

  <!-- use app.name from parent pom -->
  <!--artifactId>${app.name}</artifactId-->
  <artifactId>xdf</artifactId>

  <packaging>pom</packaging>
  <!--name>xdf</name-->
  <properties>
    <!--
    <build.finalName>${project.artifactId}-${project.version}</build.finalName>
    -->
    <build.finalName>${app.name}-${project.version}</build.finalName>
    <!-- value generated by Maven -->
    <!-- assembly ID in ..assembly.xml -->
    <ass.id>ass</ass.id>
    <ass.desc>src/main/assembly/xdf-assembly.xml</ass.desc>
    <!-- value generated by Maven -->
    <ass.out>${project.artifactId}-${project.version}-${ass.id}</ass.out>
  </properties>
  <dependencies>
    <dependency>
      <groupId>xdf-nextgen</groupId>
      <artifactId>xdf-core</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>xdf-nextgen</groupId>
      <artifactId>xdf-meta</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>xdf-nextgen</groupId>
      <artifactId>xdf-component</artifactId>
      <version>${project.version}</version>
    </dependency>		
    <dependency>
      <groupId>xdf-nextgen</groupId>
      <artifactId>xdf-preview</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>xdf-nextgen</groupId>
      <artifactId>xdf-sql</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>xdf-nextgen</groupId>
      <artifactId>xdf-data-profiler</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>xdf-nextgen</groupId>
      <artifactId>xdf-parser</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>xdf-nextgen</groupId>
      <artifactId>xdf-devkit</artifactId>
      <version>${project.version}</version>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <!-- Assembly plugin -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>${maven.assembly.version}</version>
        <executions>
          <execution>
            <id>razorsight-xdf-assembly</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <outputDirectory>${output.directory}</outputDirectory>
              <!--archiveBaseDirectory>${ass.out}</archiveBaseDirectory-->
              <descriptors>
                <!-- descriptor>src/main/assembly/xdf-assembly.xml</descriptor -->
                <descriptor>${ass.desc}</descriptor>
              </descriptors>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <!-- Assembly plugin : end -->
      <!-- RPM plugin -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>rpm-maven-plugin</artifactId>
        <version>2.1.5</version>
        <executions>
          <execution>
            <id>xdf-rpm-build</id>
            <goals>
              <goal>rpm</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <license>Synchronoss proprietary code</license>
          <distribution>XDF-${rpm.version}</distribution>
          <needarch>false</needarch>
          <targetOS>i386-linux</targetOS>
          <group>BDA/XDF</group>
          <packager>BDA</packager>

          <name>${rpm.name}</name>
          <!-- version>${rpm.version}</version -->
          <!--
            Only applicable to a Mapping if filemode,
            username, AND groupname are NOT populated.
          -->
          <defaultUsername>${app.user}</defaultUsername>
          <defaultGroupname>${app.group}</defaultGroupname>
          <defaultDirmode>755</defaultDirmode>
          <defaultFilemode>444</defaultFilemode>
          <!-- -->
          <defineStatements>
            <defineStatement>_unpackaged_files_terminate_build 0</defineStatement>
          </defineStatements>
          <mappings>
            <!-- dirs -->
            <mapping>
              <directory>${app.optDir}</directory>
            </mapping>
            <mapping>
              <directory>${app.optDir}/lib</directory>
            </mapping>
            <mapping>
              <directory>${app.optDir}/bin</directory>
            </mapping>
            <mapping>
              <directory>${app.optDir}/conf</directory>
            </mapping>
            <mapping>
              <directory>${app.varDir}/log</directory>
            </mapping>
            <!-- files -->
            <mapping>
              <directory>${app.optDir}/lib</directory>
              <directoryIncluded>false</directoryIncluded>
              <filemode>444</filemode>
              <username>${app.user}</username>
              <groupname>${app.group}</groupname>
              <sources>
                <source>
                  <location>target/${ass.out}/lib</location>
                </source>
              </sources>
            </mapping>
            <mapping>
              <directory>${app.optDir}/srv</directory>
              <directoryIncluded>false</directoryIncluded>
              <filemode>444</filemode>
              <username>${app.user}</username>
              <groupname>${app.group}</groupname>
              <sources><source>
                <location>../xdf-rest/target/xdf-rest-3.1.0_dev-all.jar</location>
              </source></sources>
            </mapping>

            <mapping>
              <directory>${app.optDir}/bin</directory>
              <directoryIncluded>false</directoryIncluded>
              <filemode>755</filemode>
              <username>${app.user}</username>
              <groupname>${app.group}</groupname>
              <sources>
                <source>
                  <location>target/${ass.out}/bin</location>
                  <includes>
                    <include>*.sh</include>
                    <include>*.py</include>
                  </includes>
                </source>
                <source>
                  <location>${project.basedir}/../xdf-rest/src/main/scripts</location>
                  <includes>
                    <include>run-task.sh</include>
                    <include>start-server.sh</include>
                  </includes>
                </source>
                <source>
                  <location>${project.basedir}/../xdf-meta/src/main/scripts</location>
                  <includes>
                    <include>xdf-mdcli.sh</include>
                  </includes>
                </source>
                <source>
                  <filter>true</filter>
                  <location>target/${ass.out}/bin/xdf_info</location>
                </source>
              </sources>
            </mapping>

            <mapping>
              <directory>${app.optDir}/conf</directory>
              <directoryIncluded>false</directoryIncluded>
              <filemode>644</filemode>
              <username>${app.user}</username>
              <groupname>${app.group}</groupname>
              <sources>
                <source>
                  <location>${project.basedir}/../xdf-core</location>
                  <includes>
                    <include>log4j.dtd</include>
                    <include>log4j.xml</include>
                  </includes>
                </source>
                <source>
                  <location>${project.basedir}/../xdf-rest</location>
                  <includes>
                    <include>logback.xml</include>
                    <include>xdf-rest.conf</include>
                  </includes>
                </source>
                <source>
                  <location>${project.basedir}/../xdf/src/main/resources/log4j.properties</location>
                </source>
              </sources>
            </mapping>

          </mappings>
          <!-- Installation scripts -->
          <preinstallScriptlet>
            <script>
#echo PreInstall done: ${app.nameVer}
            </script>
          </preinstallScriptlet>
          <postinstallScriptlet>
            <script>
echo PostInstall started: ${app.nameVer}
(
  cd ${app.optParDir} # /dfs/opt/bda
  rm -f ${app.name}-current
  ln -s ${app.name}/${app.nameVer} ${app.name}-current
)
#(cd ${app.optDir};ln -s bin sbin)
echo Soft link created:
ls -l ${app.optParDir}/${app.name}-current # ${app.optDir}/sbin
#
echo PostInstall done: ${app.nameVer}
            </script>
          </postinstallScriptlet>
          <preremoveScriptlet>
            <script>
echo PreRemove started: ${app.nameVer}
echo Remove Python compiled files
rm -f ${app.optDir}/bin/*.pyc
echo Remove soft link
rm -f ${app.optParDir}/${app.name}-current # ${app.optDir}/sbin
echo PreRemove done: ${app.nameVer}
            </script>
          </preremoveScriptlet>
        </configuration>
      </plugin>
      <!-- RPM plugin : end -->
    </plugins>
  </build>
</project>
