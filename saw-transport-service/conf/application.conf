
play.crypto.secret="changeme"
play.crypto.secret=${?APPLICATION_SECRET}

APPLICATION_SECRET="PbJ2]H^WhFEBqKT3rlNtJMY1i^PHUX>eMjiPyaC`w5RB[W]jihkhK?Vb1m`gJMw5"

# CORS filter configuration
play.filters.cors {

    # The path prefixes to filter.
    pathPrefixes = ["/"]

    # The allowed origins. If null, all origins are allowed.
    allowedOrigins = null

    # The allowed HTTP methods. If null, all methods are allowed
    allowedHttpMethods = [ "POST", "GET", "OPTIONS" ]

    # The allowed HTTP headers. If null, all headers are allowed.
    allowedHttpHeaders = null

    # The exposed headers
    exposedHeaders = []

    # Whether to support credentials
    supportsCredentials = true

    # The maximum amount of time the CORS meta data should be cached by the client
    preflightMaxAge = 1 hour
}

play.http.filters = "controllers.Filters"


# Port must be passed to play as CMD parameter, THERE IS NO DEFAULT VALUE
http.port=9101
play.i18n.langs = [ "en" ]
es = {

#  es 5
  host = "rtesap105"

# es 1.7
#  host = "rtesap201"
  timeout = 30
  port = 9200
  username = "elastic"
  password = "xuw3dUraHapret"

}

security = {

  host = "10.49.0.172"
  timeout = 30
  port = 9021
  path = "/saw-security/auth/validateToken"
  bypass = true


}

metadata = {

  path = "/main/metadata"
  zookeeper-quorum = "mapr-dev01.sncrbda.dev.vacum-np.sncrcorp.net,mapr-dev02.sncrbda.dev.vacum-np.sncrcorp.net,mapr-dev03.sncrbda.dev.vacum-np.sncrcorp.net,mapr-dev04.sncrbda.dev.vacum-np.sncrcorp.net,mapr-dev05.sncrbda.dev.vacum-np.sncrcorp.net"
  user = "mapr"

}


spark = {

  master = "spark://mapr-dev02.sncrbda.dev.vacum-np.sncrcorp.net:7077"
  executor.memory = "2G"
  cores.max = "2"
  driver.memory = "2G"

  sql-executor = {

    input-file-location = "/main/data/saw/sql-executor/input"
    result-file-location = "/main/data/saw/sql-executor/results"
    script = "/opt/saw/sql-executor/bin/run.sh"
    wait-time = 5

    inline-data-store-limit-bytes = 268435456
    inline-data-store-limit-rows = 10
  }


}

