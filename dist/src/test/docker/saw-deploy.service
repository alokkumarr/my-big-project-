[Unit]
Description=Deploy SAW application
After=default.target

[Service]
Type=oneshot
ExecStart=/root/deploy
WorkingDirectory=/maven
Environment=SAW_ENV=dev
SyslogIdentifier=saw-deploy

[Install]
WantedBy=default.target
