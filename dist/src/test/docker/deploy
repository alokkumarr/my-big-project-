#!/bin/sh
#
# Deploy SAW Services into localhost
#
set -eu

tar -xzf saw-services-*.tgz
echo "Starting deployment"
ansible-playbook -i /root/config saw-services-*/ansible/site.yml
echo "Finished deployment"
