- name: Product module metadata
  uri:
      url: "https://localhost:9800/internal/proxy/storage/product-module/{{ sip_product_module_id }}"
      method: POST

      src: "{{ SIP_PRODUCT_MODULE }}/file.json"
      # body: "{{ lookup('file','issue.json') }}"
      remote_src: false
