info:
  component: Synchronoss Gateway Service
spring:
  application:
    name: saw-gateway-service
server:
  address: localhost
  port: 8080 
  context-path: /saw
  compression:
    enabled: true    
   
  


 